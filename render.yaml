services:
  - type: web
    name: brainbrew
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn wsgi:app
    envVars:
      - key: QUIZ_DB_PATH
        value: quiz.db
      - key: SECRET_KEY
        generateValue: true
